import { BkgColorDirective } from './bkg-color.directive';

describe('BkgColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BkgColorDirective();
    expect(directive).toBeTruthy();
  });
});

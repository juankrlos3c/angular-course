import { UpperPipePipe } from './upper-pipe.pipe';

describe('UpperPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UpperPipePipe();
    expect(pipe).toBeTruthy();
  });
});
